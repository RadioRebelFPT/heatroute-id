# OpenRouteService API key (get one free at https://openrouteservice.org/sign-up/)
# Server-side only — read by /api/ors-proxy (Vercel serverless function in
# prod, Vite dev proxy in dev). NOT exposed to the client bundle.
ORS_API_KEY=
